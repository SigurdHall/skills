display_name: Power BI PBIP
short_description: Work safely with PBIP, report JSON, semantic TMDL, themes, bookmarks, and UTF-8 without BOM.
default_prompt: Use this when editing Power BI PBIP projects, report JSON, semantic model TMDL, themes, bookmarks, relationships, or templates.
